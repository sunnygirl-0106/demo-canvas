# 视频交互改版验收记录

日期：2026-09-11

## 自动化验证

- `npm test`：5 个测试文件、30 项测试全部通过。
- `npm run build`：TypeScript 检查及 Vite 生产构建通过。
- 覆盖：模式草稿恢复、素材角色与有效输入、首帧移除不自动补位、模型兼容、源资产与源文件变更、不可变任务快照。
- 时间轴覆盖：15.1 秒源在 3 秒处建立 03–07 秒，片尾建立 11–15 秒；4 秒源只有 00–04 秒；未知、过短、过长源不可提交；端点拖动与平移始终保持整数秒及最短 4 秒。
- 元数据使用媒体完整时长；整段任务保留小数尾部，片段任务只记录整数秒范围。

## Chrome 页面验证

- 首尾帧空槽与有素材均为 72×60 px；首帧悬浮时，尾帧位置和宽度不变。
- 悬浮预览浮层可从缩略图连续移入并点击；图片大图正常打开，Esc 关闭。直接预览关闭后焦点返回素材按钮。
- 首帧移除后尾帧保留，交换按钮禁用；可通过选择菜单重新添加并交换。
- 首尾帧模式只显示两张图片，@ 菜单只包含它们，以四字母名称插入引用。
- 使用本地 5.088005 秒视频验证点击建选区、右端拖至 00–05、清除、片尾重新定位到 01–05；用户文字保持不变。
- 播放选区后，播放器在 5 秒终点暂停；选区时间与输入上下文同步。
- 无修改文字或无选区时生成不可用；提交后的演示记录明确显示未生成或修改视频。
- 不兼容的 Seedance 2.0 模型置灰并显示具体原因。
- 延长方向必须显式选择；原片约 5.1 秒、新增 30 秒时显示理论总长约 35.1 秒，不自动拼接。
- 参数浮层可修改清晰度和声音，摘要即时更新，Esc 关闭保留已选值。
- 源视频节点上的「编辑视频」入口新建下游任务并绑定原视频，原视频保留。
- 常规桌面及 640×720 窗口检查通过；窄窗口中时间轴、输入框、生成按钮可同时看到，面板不会被画布工具条遮挡，参数浮层未被屏幕裁切。

## 验证限制

Chrome 扩展未开启文件 URL 访问权限，自动化文件选择在 `setFiles` 阶段被拒绝，因此未完成上传的浏览器端到端验收。视频元数据读取与过期文件回调保护已通过单元测试。外部文件拖入已接入画布，但本次未执行操作系统文件拖入的自动化验证。

本轮不调用真实模型、不编码裁切、不自动拼接；生成按钮记录演示任务及其输入、角色、范围、方向和参数。状态保存在内存，刷新或切换示例场景会重置。
