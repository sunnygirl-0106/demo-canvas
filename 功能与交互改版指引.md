# 视频节点功能与交互改版指引

版本：2026-09-11｜用途：交给 Codex 实现前端 PRD 演示原型

## 1. 目标与范围

在现有 `demo-canvas` 仓库上改进视频生成与编辑体验。继续使用现有画布、节点、素材和本地媒体，重点重做视频面板的信息层级、素材交互、片段选择和参数设置。

这是一份实现规划。当前仓库未接入真实生成服务，本轮不要求接入模型、付费调用、部署或实现完整剪辑软件。不要把模拟视频结果包装成真实 AI 编辑结果。

参考：`../design/design` 的四版交互用于对照；借鉴独立悬浮信息不挤动其他素材的方式，不直接照搬其竖排标签、锁定流程和复杂工具栏。

### 已明确的用户要求

- 当前模式不使用的素材，不在当前面板展示。
- 首尾帧采用紧凑小框，减少常驻说明文字，通过悬浮反馈和 tooltip 补充信息。
- 保留素材悬浮时与画布连线的联动，并支持放大预览。
- 支持点击时间轴选择待编辑片段，片段最少 4 秒；选择后自动插入资产和时间范围，用户补充修改要求。
- 延长必须区分向前、向后。
- 模型选择单独展示；其余参数点击摘要后统一在弹窗中调整。
- 真实产品资产使用随机四个英文字母名称。@ 是选择已连接资产，不表示排序编号。
- 本轮以清晰、好看、好用为目标，不大幅重写既有业务文案。

### 本规划采用的首版默认值

以下是为了让实施有明确依据的产品建议，不代表用户已经逐项确认：

- 一次任务只选择一个编辑片段，不做多片段批量重拍。
- 点击时间轴默认建立 4 秒选区，允许拖动两端调整。
- 本轮先做时间范围编辑；暂停帧框选、涂抹、箭头、标注图等局部对象标注暂缓。
- 视频节点上的“编辑视频”和“延长视频”采用并列入口，可共用面板外壳，但任务状态独立。
- 编辑提供“整段视频”和“选取片段”；选取片段默认产出独立片段，不自动替换回原片。

## 2. 面板结构

从上到下保持四层：

1. 当前创作模式或操作入口。
2. 当前实际使用的素材；片段编辑时提供时间轴。
3. 提示词及资产、时间范围引用。
4. 底栏：独立模型选择、参数摘要、预估消耗、生成按钮。

模型与参数必须是两个独立的点击区域。例如：

`[Seedance 2.5 ▾]   [720p · 5s · 随首帧 ▾]                         [生成]`

避免将模型、任务方向、所有参数和余额挤进同一个弹窗或一行密集图标中。余额不必在面板常驻，预估消耗可以保留。

## 3. 素材区

### 3.1 只展示本次使用的素材

- 删除可见的“未使用”素材堆及其标签。
- 模式切换保留各模式草稿，包括素材角色、提示词和参数；切回时恢复。
- 隐藏素材不参与本次模拟任务参数，不出现在当前模式的 @ 候选列表中。
- 保留画布节点和历史连接，不因切换模式删除原资产。
- 已有连接需要区分本次是否生效：当前有效连接正常显示，非当前输入弱化；本次素材的悬浮联动只高亮对应有效连接。
- 更换源视频时，清除与旧视频绑定的选区和时间范围引用，保留用户文字草稿，并提示重新检查适用范围。

### 3.2 缩略图与空槽

- 首尾帧以 72×60 px 作为首版尺寸起点；视频主素材可用 80×60 px，参考素材可用 60×50 px。
- 同一角色的空槽和有素材状态尺寸一致，上传、替换不引起邻近内容跳位。
- 首尾帧固定左起右止；中间保留小型交换按钮，只有两张图都存在时可用。
- 不常驻“尾帧”“可选·连接第二张图片”等文字。空槽用加号和起止位置符号；hover/focus 提供“添加首帧”“添加尾帧”。
- 已有素材 hover/focus 时说明角色、资产名，并出现预览及更多操作入口。
- 视频保留播放标识和真实时长，避免与图片混淆。
- 缩略图允许居中裁切；预览完整展示原始比例，不拉伸素材。
- 尾帧缺失时不阻止首帧生成；提示词占位文案也要适配单首帧状态。
- 移除首帧后不悄悄把尾帧提升为首帧，要求明确补充首帧或通过操作调整角色。

### 3.3 悬浮、预览与替换

- 鼠标进入立即高亮对应连线；图片轻微上浮约 2 px、放大约 4%。
- 动效以 150–200 ms 为起点，只改变视觉变换，不改变布局宽高，不推挤相邻素材。
- 普通素材不持续摇摆，不默认堆叠倾斜。
- 停留约 300 ms 后，上方出现小浮层：资产名、必要媒体信息和“预览”入口。不要写“是否预览”。
- 鼠标可连续移入浮层；离开整个交互区域后延迟约 150–200 ms 关闭，避免入口消失。
- 点击素材本身可直接预览；点击空槽才进入添加素材。
- 图片打开完整大图；视频打开可播放、拖动进度和调节声音的播放器。
- Esc 关闭，恢复原画布位置与操作焦点；浮层靠近屏幕边缘时自动避让。
- 替换、移除放在明确的次级操作中，不需要先解锁主视频。
- 使用按钮、可访问名称、键盘焦点反馈；触屏通过点击进入，不依赖 hover。

### 3.4 拖动和 @ 引用

- 保留画布节点拖动、连接和外部文件拖入。
- 取消面板中主视频与参考素材的拖动互换，取消“拖动可改序号”。
- 首尾帧互换使用专用按钮；角色替换使用明确的选择操作。
- @ 候选项展示缩略图、四字母资产名和素材类型，引用绑定稳定资产 ID。
- 不用 @1、@2 表达素材顺序，不把参考素材排列暗示为镜头播放顺序。
- 若旧 demo 保留数值引用作为临时实现，应在本轮展示数据中改成符合真实产品的资产命名；不围绕“编号排序”增加新功能。

## 4. 视频编辑与片段选择

### 4.1 入口与默认状态

- 已有视频节点上提供“编辑视频”。进入后明确显示正在处理的源视频。
- 编辑范围提供“整段视频 / 选取片段”。“整段视频”保持现有直接填写修改要求的轻量路径。
- 选择“选取片段”后展开预览和时间轴，底部输入面板继续可见。
- 在源视频元数据准备好之前，不允许选择范围或提交任务。
- 本轮 Seedance 2.5 演示先覆盖 4–30 秒源视频。小于 4 秒或超过当前支持范围时显示原因；长视频导入后裁切成合法输入属于后续扩展。

### 4.2 点击与拖动规则

- 首次点击时间轴素材轨道，在点击附近建立默认 4 秒选区。
- 首版选区端点按整数秒吸附，以匹配已核实的时间戳能力；不要提供毫秒或逐帧精度承诺。
- 设源视频实际时长为 D，可选整数秒终点上限 E=floor(D)。点击时间 t 后，默认起点为 clamp(floor(t), 0, E−4)，终点为起点+4。
- 例如 15.1 秒原视频，在 3 秒处点击选中 03–07 秒；在接近结尾处点击选中 11–15 秒。
- 拖动左右手柄调整起止时间；最短 4 秒，最长不超过源视频允许范围，手柄不能交叉。
- 拖动选区主体平移整个范围，保持长度；到边界时停止，不溢出视频。
- 一次只有一个选区。点击选区外重新定位同一选区；点击选区内定位播放头。
- 预览播放器的进度条负责定位和播放，选区手柄负责调整范围；拖动选区不能带动整个画布。
- 显示起止时间与选中时长，区分原片时长。例如“00:03–00:08 · 已选 5 秒”，原片仍显示真实 15.1 秒。
- 提供“播放选区”和“清除选区”；播放选区到终点暂停。清除后继续停留在片段选择状态，不自动变成整段编辑。
- 原视频若包含小数秒尾部，整段编辑保留完整输入；整数秒片段模式不把剩余尾部偷偷包含进选区。

### 4.3 自动生成输入上下文

选择完成后，在提示词输入区自动显示结构化前缀，例如：

`把 [视频 ABCD] 中 [00:03–00:08] 的……`

- 视频资产和时间范围显示为可辨认的引用块，后面由用户填写修改要求。
- 时间范围引用块可点击返回选区；拖动范围时同步更新，不重复插入多条前缀。
- 用户已经输入的修改要求不因重新选区被覆盖。
- 更换源视频或清除选区时同步移除失效的时间引用。
- 自动前缀不算有效修改要求。只选片段、不填写改动时不能提交，并提示“描述想要修改的内容”。
- 可用占位示例：“将右侧黄色椅子改为红色”。不使用“留空等于原样重跑一次”。
- 若需要“再次生成”，应另设入口复用已有生成设置；上传视频没有原生成设置，不能承诺原样重跑。

### 4.4 输出含义与实现边界

- 整段编辑：目标输出为编辑后的整条输入视频。
- 片段编辑：首版建议输出编辑后的独立片段，原视频保留；暂不自动拼回原片，也不保证未选区域逐帧不变。
- 时间戳是语义约束，不能宣传为逐帧精准控制。
- 未来真实接入时，“提交完整视频并在提示词指定时间段”和“先裁切选区再提交”是两条不同路径，不能混用。
- 若采用本规划的独立片段输出，真实接入优先评估：保留原片时间范围用于界面追溯，先提取所选片段作为模型输入，再按片段自身时间组织指令。4 秒是本轮最短选区规则，也符合 2.5 待编辑视频的最低输入时长。
- 本轮前端必须真实完成选择、预览定位、范围约束和任务状态记录；不强制实现视频裁切编码或 AI 修改。
- 模拟生成需要标明演示状态。不得用固定默认视频加上伪造的时长来冒充真实选区编辑结果。

## 5. 视频延长

- 在视频节点上提供独立“延长视频”入口，进入后仍使用同一套素材、提示词和底栏视觉语言。
- 明确显示“向前延长 / 向后延长”，首次进入未选择方向时不允许提交。
- 用简洁关系示意解释方向：向前为“新增 → 原视频”，向后为“原视频 → 新增”。
- 方向靠近源视频展示，不藏进通用参数弹窗。
- 生成时长在参数弹窗调整；主面板保留摘要。
- 删除当前“每点击一次加 2 秒、达到上限又归零”的逻辑。
- 区分原视频时长、生成片段时长与拼接总时长；不要沿用“延长上限=30−原片时长”的计算。
- 编辑和延长是互斥的本次任务意图。切换时保留各自草稿，编辑中的时间选区不带入延长任务。
- 源视频同时可以有其他参考素材，但必须明确哪一条是被延长的视频。
- 本轮先支持基于整条源视频续写，不增加续写前的再次裁切流程。
- 未来后端根据方向生成相应指令；不要求用户在自由文本里再次手写方向。
- 是否自动拼接完整成片需要真实接口验证后决定，本轮不承诺自动拼回。

## 6. 模型与参数

### 6.1 独立模型选择

- 模型选择器单独列出，例如“Seedance 2.5 ▾”。不和清晰度、比例、时长摘要合成一个按钮。
- 保留可识别的品牌与版本，不显示完整接口模型 ID，也不只显示“2.5”。
- 去掉装饰图标和无行为的麦克风等入口。
- 按仓库已支持模型实现。Kling 等可作为未来品牌扩展，不新增未经核实的可用模型能力。
- 有片段编辑配置时，不兼容模型置灰并给出具体原因；列表仍可打开查看。
- 提供退出当前编辑操作的路径，保留原素材与草稿；不能静默清空配置或悄悄转换任务。
- 模型能力采用配置表管理，避免只根据品牌名称猜测支持范围。

### 6.2 参数弹窗

- 点击参数摘要，打开锚定于面板的设置浮层；靠近屏幕边缘自动调整方向。
- 统一包含清晰度、时长、比例、声音；只显示当前模式适用项。
- 编辑时，“随原片 / 随所选片段”显示为只读信息；没有必要重复多个锁图标。
- Seedance 2.5 首尾帧模式的比例显示“随首帧”；编辑与延长显示“随原片”。
- 可调项使用明确选中态，立即保存到当前草稿并更新摘要；Esc 或点击外部关闭，不回滚已经选择的值。
- 声音只有一个明确开关，不重复显示“声”、喇叭、音量等多个入口。
- 固定的 788 等消耗数字应改为明确的演示估算或移除；不要暗示是真实价格。参数更改与展示状态必须一致。
- 必要的模型约束在选择时说明，不等用户点击生成才集中报错。

## 7. 视觉与文案规范

- 延续深色画布，使用中性深灰面板；正文对比清晰，辅助说明可读，避免大量极小低对比文字。
- 统一间距、圆角、描边和控件高度，减少面板内层层套框。
- 强调色集中用于选中态、联动连线和生成按钮；不让每个图标都发光。
- 主面板只展示当前任务必要信息，文件名、角色解释、完整媒体信息放入 tooltip 或预览。
- 主面板不要常驻技术实现词，例如 API 字段、hover、素材落位、内部区域名。
- “参考图”可小幅调整为“参考素材”，因为实际包含视频。
- 暂不扩展“超长视频”。若现有入口只有固定 30 秒参数差异，建议并入普通时长选项；若保留入口，不能虚构分段或续写能力。
- 减少动作歧义：点击已有素材=预览，点击加号=添加，明确菜单项=替换/移除。
- 模态预览与弹窗保持键盘可操作，关闭后恢复焦点；尊重减少动态效果的系统设置。

## 8. 实施顺序与验收

### 实施顺序

1. 调整任务状态与模式草稿：整段编辑、片段编辑、延长方向、参数配置分开维护。
2. 重做紧凑素材区：移除未使用区、取消无意义拖动、补齐预览和替换。
3. 完成片段选择：真实媒体时长、点击建选区、拖动、范围约束和输入引用同步。
4. 完成独立模型选择与参数弹窗。
5. 接通模拟任务记录与生成状态，补齐空态、错误提示、退出恢复和视觉细节。

### 必须通过的验收场景

- 首尾帧空态、只有首帧、两张图、交换、替换、移除的布局稳定，当前不使用的素材完全不占面板空间。
- 素材 hover 不推挤邻居；鼠标可以从素材移到预览入口；预览关闭不改变画布位置。
- 模式切换后原素材仍在画布，草稿可以恢复；本次有效输入与面板展示一致。
- @ 菜单能识别四字母资产名，引用指向稳定资产，不依赖列表序号。
- 15.1 秒视频在 3 秒处点击得到 03–07 秒；靠近结尾得到 11–15 秒；选区时长不能小于 4 秒。
- 4 秒视频只能选 00–04 秒；小于 4 秒、未知时长、超出首版范围的视频有明确处理。
- 拖动两端、平移选区、重新点击定位、清除、切换源视频后，选区与输入时间引用保持一致。
- 修改选区不丢失用户已经填写的要求；只有自动前缀时不能生成。
- 拖动时间轴不会拖动画布；播放选区到终点暂停。
- 整段编辑与片段编辑的目标输出含义清楚，不展示“原样重跑”等不确定承诺。
- 延长必须选择方向；编辑选区不污染延长状态；生成时长与原片时长不混淆。
- 模型与参数是两个独立入口；不兼容模型有明确原因和退出路径。
- Seedance 2.5 首尾帧、编辑、延长模式不出现可自由选择的固定输出比例。
- 参数摘要反映当前值，所有看起来能操作的控件有真实行为；未实现能力不伪装成可用。
- 在常用桌面窗口及较窄窗口中无主要控件溢出、弹窗截断或关键操作被遮挡。

测试重点是状态转换、片段边界、引用同步和任务参数正确性。现有相关测试应更新，新增测试覆盖这些有实际风险的逻辑；视觉和 hover 通过真实浏览器验证，无需为每个间距或装饰效果写测试。最后执行仓库适用的测试和构建检查。

## 9. 仓库落点建议

- `src/generator/VideoPanel.tsx`：视频面板、任务分支、素材与底栏整合。
- `src/generator/MaterialRow.tsx`、`materialLayout.ts`：紧凑布局、角色、预览入口和拖动收敛。
- `src/store/generator.ts`：模式草稿、源视频、选区、方向和参数状态。
- `src/generator/PromptBox.tsx`：资产引用及时间范围上下文与自由文字的协同。
- `src/generator/BottomBar.tsx`：模型和参数的独立入口、估算及生成状态。
- `src/nodes/VideoToolbar.tsx`：节点上的编辑、延长入口。
- `src/nodes/media.ts`：复用真实视频元数据能力。
- `src/styles/app.css`、`tokens.css`：统一视觉和状态反馈。
- 片段选择器、媒体预览、参数浮层可拆为独立组件，避免把全部逻辑继续塞进单个面板。

先核实现有结构再修改；不要重建整个仓库，不迁移无关功能，不接入新的建站或视频生成平台。

## 10. 已核对能力与待验证边界

| 内容 | 结论 |
|---|---|
| 2.5 时间范围编辑 | 官方支持通过时间戳表达生效时段，指南说明响应整数秒；不承诺逐帧精准 |
| 2.5 编辑任务 | `omni_reference_task_type=edit`，`ratio=adaptive`，`duration=-1`，待编辑视频 4–30 秒 |
| 2.5 延长任务 | 支持向前/向后，`extend`，比例随源视频；生成时长 4–30 秒或自动 |
| 暂停帧框选对象 | 截图证明产品存在该交互；公开文档未核实同款标注专用接口，本轮暂缓 |
| 片段最少 4 秒 | 用户观察到的竞品规则；本轮明确采用，与 2.5 编辑输入最低时长一致 |
| 框外、选区外完全不变 | 不作保证，不能仅通过提示词承诺逐帧保真 |
| 自动替换回原片、延长后拼接 | 需确定媒体处理方案和实际产物行为，本轮暂缓 |
| 其他模型的编辑兼容性 | 按模型分别核实，不从竞品置灰状态推导全部模型能力 |

官方资料（2026-09-11 核对，真实接入前需复核）：

- [Seedance 2.5 教程](https://docs.volcengine.com/docs/82379/2607688?lang=zh)
- [Seedance 2.5 提示词指南](https://docs.volcengine.com/docs/82379/2607689?lang=zh)
- [创建视频生成任务](https://docs.volcengine.com/docs/82379/1520757?lang=zh)

本轮完成标准：用户能够在现有画布中清楚地完成添加素材、预览、选择编辑片段、填写要求、选择延长方向、单独切换模型和调整参数，并理解当前模拟任务将处理什么、预期输出什么。
